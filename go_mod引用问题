### go_mod引用问题

问题1，特别注意首次go get xxxxxx@版本id，可能由于版本同步问题，第一时间不能及时更新成功，会提示当前版本不存在。

问题2，go mod 引入本地其他包
```
比如：
包A
包B

module 包A

go 1.16

require (
	包B v0.0.0
)

replace 包B => ..\包B
```
这里必须是v0.0.0这种格式，否则是不会成功的。
